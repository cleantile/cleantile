- var opts = {title: "<cleantile-split> tests", path: "../../../"}
include "../../lib/test" exposing opts

replace imports

replace tests
  :coffeescript
    WCT.loadSuites [
      "./test-split-layout.html"
    ]
